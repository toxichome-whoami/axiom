version: "3.9"

services:
    axiom:
        build: .
        ports:
            - "4500:4500" # HTTP / REST API
        env_file:
            - .env
        volumes:
            - ./logs:/app/logs
        restart: unless-stopped
